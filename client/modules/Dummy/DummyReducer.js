const initialState = { data: [] };

const DummyReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// Export Reducer
export default DummyReducer;
